meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}auth/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "dev@gmail.com",
      "password": "string1"
  }
}

script:post-response {
  bru.setEnvVar("API_KEY",res.body.accessToken);
  bru.setEnvVar("API_KEY_REFRESH", res.body.refreshToken);
  
  // if env var is null => this code created 
}
